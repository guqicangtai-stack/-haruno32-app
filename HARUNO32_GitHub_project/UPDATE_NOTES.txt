HARUNO32 v6.1.0
- Deletes the known truncated API key from older versions
- Never embeds or restores that invalid key
- Keeps the correct Supabase Project URL automatically
- Removes spaces and line breaks when a key is pasted
- Detects keys ending in '-' as truncated before sending
- Shows URL, masked key, key length, key type and server error on screen
- Keeps local records safe while connection is incomplete
